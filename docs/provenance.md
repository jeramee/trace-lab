# TraceLab provenance manifest

TraceLab v0.1 writes `provenance_manifest.json` to record local evidence origin for the simulated run.

The provenance record is operational trace metadata only. It does not validate scientific truth, approve execution, complete human review, execute hardware, or promote claims.

`provenance-summary` provides an operator-facing summary and can write `provenance_summary.json`.
