# Replay Plan Manifest

TraceLab v25 adds `replay_plan_manifest.json` and `replay_plan_summary.json`.

These files are local operator checklist artifacts only. They list bounded TraceLab CLI commands and required inputs for replaying the simulated evidence shape. They do not execute replay, retry hidden actions, call hardware, call networks, install packages, validate scientific truth, or promote claims.
