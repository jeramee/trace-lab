# Separation

`trace-lab` is the adapter-aware instrumented research evidence orchestrator.

It may later hand evidence to `evidence-ai-core` and `run-lab`.

It must not become a notebook workbench and must not implement physical hardware control in this rough scaffold.
